# AGENTS.md

## Purpose

This repository stores reusable, multi-agent skills.

## Rules

- Treat `skills/<name>/core/` as canonical behaviour.
- Keep adapter files minimal and focused on agent-specific execution details.
- Update `skill.yaml` version for each behaviour change.
- Keep references and tests in sync with workflow changes.
- Run validation before opening PRs.
